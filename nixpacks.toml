# nixpacks.toml
[providers]
php = "8.2"

[phases.build]
cmds = [
    "composer install --no-dev",
    "php artisan config:cache"
]

[phases.start]
cmd = "php artisan serve --host 0.0.0.0 --port $PORT"